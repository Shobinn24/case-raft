{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "flask db upgrade && echo '>>> Migration done, starting Gunicorn on port '$PORT && gunicorn run:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120 --preload --log-level debug --capture-output --access-logfile - --error-logfile -",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
